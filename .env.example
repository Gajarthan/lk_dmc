# Set these in your process environment. The CLI does not auto-load this file.
OPENCODE_BASE_URL=https://your-opencode-server.example
OPENCODE_USERNAME=opencode
OPENCODE_PASSWORD=
# Optional provider/model. Leave empty to use the server default.
OPENCODE_MODEL=
